{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Feedback list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Timetocall</th>
                <th>Phonenumber</th>
                <th>Email</th>
                <th>Question</th>
                <th>Connectiontype</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('feedback_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{% if entity.timeToCall %}{{ entity.timeToCall|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.phoneNumber }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.question }}</td>
                <td>{{ entity.connectionType }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('feedback_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('feedback_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('feedback_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
